<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Assignment1_App" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/accentColor</item>
        <item name="colorPrimaryVariant">@color/accentColor</item>
        <item name="colorOnPrimary">@color/fontColor</item>

<!--        &lt;!&ndash; Action Bar &ndash;&gt;-->
        <item name="android:actionBarTheme">@style/ActionBarTheme</item>
<!--        <item name="android:actionBarStyle">@style/ActionBarStyle</item>-->
<!--        <item name="actionBarStyle">@style/ActionBarStyle</item> &lt;!&ndash; For compatibility with AppCompat &ndash;&gt;-->

        <!-- Window background -->
        <item name="android:windowBackground">@color/bgColor</item>

    </style>

    <style name="Theme.Assignment1_App" parent="Base.Theme.Assignment1_App" />

    <!-- Define ActionBar theme -->
    <style name="ActionBarTheme" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:background">@color/actionBarBg</item> <!-- Set action bar background color -->
        <item name="android:titleTextStyle">@style/ActionBarTitleText</item> <!-- Set title text style -->
    </style>

    <!-- Define ActionBar style -->
    <style name="ActionBarStyle" parent="Widget.AppCompat.ActionBar.Solid">
        <item name="android:background">@color/actionBarBg</item> <!-- Set action bar background color -->
    </style>

    <!-- Define ActionBar title text style -->
    <style name="ActionBarTitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textColor">@color/fontColor</item> <!-- Set action bar title font color -->
    </style>

</resources>
